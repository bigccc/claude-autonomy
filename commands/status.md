---
description: "View current autonomy system status and task progress"
allowed-tools: ["Bash(${CLAUDE_PLUGIN_ROOT}/scripts/show-status.sh)"]
---

# Autonomy Status

Execute the status script:

```!
"${CLAUDE_PLUGIN_ROOT}/scripts/show-status.sh"
```

Present the status information clearly. If there are failed or blocked tasks, suggest possible actions.
