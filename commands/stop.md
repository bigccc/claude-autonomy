---
description: "Stop the autonomous development loop"
allowed-tools: ["Bash(${CLAUDE_PLUGIN_ROOT}/scripts/stop-loop.sh)"]
---

# Stop Autonomous Loop

```!
"${CLAUDE_PLUGIN_ROOT}/scripts/stop-loop.sh"
```

Confirm to the user that the loop has been stopped. Suggest `/autocc:status` to review progress.
